import 'package:flutter/material.dart';

class CreateApartment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Apartment'),
      ),
      body: Center(
        child: Text('Create Apartment Screen'),
      ),
    );
  }
}
