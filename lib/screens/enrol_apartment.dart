import 'package:flutter/material.dart';

class EnrolApartment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Enrol Apartment'),
      ),
      body: Center(
        child: Text('Enrol Apartment Screen'),
      ),
    );
  }
}
